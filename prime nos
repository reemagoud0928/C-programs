#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,b,i,j,flag;
    scanf("%d%d",&a,&b);
    for(i=a;i<=b;i++){
        if(i==1 || i==0)
             continue;
        flag=1;
 for(j=2;j<=i/2;++j) 
        {
            if(i%j==0) 
            {
                flag=0;
                break;
            }
        }
        if(flag==1)
            printf("%d ",i);
    }

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
